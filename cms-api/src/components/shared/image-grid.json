{
  "collectionName": "components_shared_image_grids",
  "info": {
    "displayName": "image-grid"
  },
  "options": {},
  "attributes": {
    "images": {
      "type": "media",
      "multiple": true,
      "required": true,
      "allowedTypes": [
        "images"
      ]
    },
    "columns": {
      "type": "enumeration",
      "default": "three",
      "enum": [
        "two",
        "three",
        "four"
      ]
    },
    "layout": {
      "type": "enumeration",
      "default": "equal",
      "enum": [
        "equal",
        "masonry"
      ]
    }
  },
  "config": {}
}
